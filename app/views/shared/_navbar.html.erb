<nav class="navbar navbar-expand-lg navbar-dark  bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">MANUALAUTH</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
            <% if logged? %>

                <li class="nav-item">
                    <%= link_to current_user.name, user_path(current_user.id), class:'nav-link' %>
                </li>

                 <li class="nav-item">
                    <%= link_to 'Cerrar', session_path(current_user.id), method: :delete, class:'nav-link'%>
                </li>

            <% else %>

                <li class="nav-item">
                    <%= link_to 'Registrate', new_user_path, class:'nav-link'%>
                </li>   
                <li class="nav-item">
                    <%= link_to 'Iniciar Session', new_session_path, class:'nav-link'%>
                </li>
            <% end %>
        </ul>
        </div>
    </div>
</nav>